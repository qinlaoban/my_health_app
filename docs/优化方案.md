# my_health_app 优化方案

> 自动生成于 2026-07-01

---

## P0 — 严重问题（建议立即修复）

| # | 问题 | 文件 | 方案 |
|---|------|------|------|
| 1 | HealthProvider 与 HealthService 无交互 | `lib/providers/health_provider.dart` vs `lib/services/health_service.dart` | HealthProvider 集成 HealthService，统一管理健康数据流 |
| 2 | 死代码：home_screen.dart 未使用 | `lib/screens/home_screen.dart` (245行) | 删除整个文件 |
| 3 | 死代码：scatter_chart_widget.dart 未使用 | `lib/widgets/scatter_chart_widget.dart` (483行) | 删除整个文件 |
| 4 | 测试文件不通过 | `test/widget_test.dart` | 重写或删除模板测试 |
| 5 | 死依赖 sqflite + path | `pubspec.yaml` | 移除未使用的依赖 |

---

## P1 — 架构优化

| # | 问题 | 方案 |
|---|------|------|
| 6 | 数据模型定义分散 | 统一放到 `lib/models/`，删除 `health_provider.dart` 中的内联定义，去重 `HealthBubble` |
| 7 | 5 个 Tab Widget 大量重复代码 | 提取公共 Widget 到 `lib/screens/widgets/common/`（`InsightCard`、`SummaryCard`、`ChoiceChip`） |
| 8 | 导航不统一 | 全部统一为 `context.go()` / `context.push()`，禁用 `Navigator.pushNamed` |
| 9 | Provider 粒度过粗 | 将 `HealthProvider` 拆分为 `UserProvider` + `HealthRecordProvider` + `MedicalRecordProvider` |
| 10 | 硬编码 Mock 数据 | 打通 Provider-数据源链路，移除 `_mockData` |

---

## P2 — 代码质量

| # | 问题 | 方案 |
|---|------|------|
| 11 | HealthKit 不可用时无处理 | Android/模拟器显示友好提示而非白屏 |
| 12 | import 路径不统一 | 全部使用 package 路径 |
| 13 | 静态分析告警（Key?、const 等） | 运行 `dart analyze` 逐条修复 |
| 14 | 网络头像不稳定 | 换成本地 Asset 或 `cached_network_image` |
| 15 | Column+SingleChildScrollView 长列表 | 改用 `ListView.builder` |

---

## P3 — UI/UX 优化

| # | 建议 |
|---|------|
| 16 | 添加全局 Loading / Skeleton 骨架屏 |
| 17 | 空状态页面优化 |
| 18 | 暗色模式细节完善（卡片颜色适配） |
| 19 | 添加 Semantics 无障碍标签 |

---

## 新功能（按价值排序）

| 优先级 | 功能 | 复杂度 | 说明 |
|--------|------|--------|------|
| ★★★ | 血糖追踪 | 低 | 数据模型已有 `bloodGlucose`，缺 UI 展示 |
| ★★★ | 健康数据手动录入 | 中 | 步数、体重、血压等可手动输入 |
| ★★★ | 目标设定与进度追踪 | 中 | 步数目标、体重目标、饮水目标 + 进度圆环 |
| ★★★ | 用药提醒 + 本地推送通知 | 中 | `flutter_local_notifications` 实现真推送 |
| ★★☆ | 健康评分模型 | 低 | 基于多维指标的综合评分（雷达图已有基础） |
| ★★☆ | 数据导出 PDF/CSV | 低 | 生成健康报告分享给医生 |
| ★★☆ | 趋势异常检测 | 中 | 心率/血压超标时告警 |
| ★☆☆ | 周报/月报生成 | 高 | AI 总结 + 图片分享 |
| ★☆☆ | Onboarding 引导页 | 低 | 首次安装功能介绍 |
| ★☆☆ | 多语言支持 | 中 | Flutter l10n |

---

## 推荐执行顺序

```
第一波（整理）→ 第二波（架构）→ 第三波（新功能）
   P0 #1~5        P1 #6~10        ★★★ 优先
   P2 #11~15      P3 #16~19       ★★☆ 次之
                                   ★☆☆ 按需
```
